# cra-template

This is the official template for [The React App](https://github.com/elandamor/the-react-app).

To use this template, add `--template @elandamor/cra-template` when creating a new app.

For example:

```sh
npx create-react-app my-app --template @elandamor/cra-template
```
