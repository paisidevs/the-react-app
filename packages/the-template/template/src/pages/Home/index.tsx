import { H1, ScrollView } from '@elandamor/tra-components';
import React from 'react';

const Home: React.FC = () => {
  return (
    <ScrollView>
      <H1>the-react-app</H1>
    </ScrollView>
  );
};

export default Home;
